DirectoryStructure (
    contents: [
        (
            name:"main.tox",
            text: "import foo::bar; fn main() {}"
        ),
        (
            name:"foo.tox",
            text: "export fn bar() {}"
        )
    ]
)