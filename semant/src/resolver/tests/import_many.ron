DirectoryStructure (
    contents: [
        (
            name:"main.tox",
            text: "import foo::{bar,baz}; mod foo; fn main() {}"
        ),
        (
            name:"foo.tox",
            text: "export fn bar() {}  export fn baz() {}"
        )
    ]
)