import foo::bar::{bar,Foo};

type ParserResult<T> = Result<T,Foo>;

type Foo = i32;

mod bar;
mod foo;

export fn main()  { 
    let x= 10;
    let x =10;
    let x =x;

    let x:i64 = 19;
    v=id::<i32>();

    for(let i=0;i<10;i+=1) {
        print("ok");
    };
    if true {print("good")} else {print("bad")};
}




export fn id<T>(a:T,(x,x):T) -> (T,i32) {
    
}