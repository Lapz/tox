class Math {
  fn add(a:int,b:int) -> int {
      return a+b;
  }
}

class Person {
  name:str,surname:str;
  
  fn hello(self:Person) {
    print "Hello" + " " + self.name + " "+ self.surname;
    return;
  }

}

fn main() -> int {
  let me = Person {name:"Lenard",surname:"Pratt"};
  
  print me.hello(me);
  print Person.add(10,2);
  return 0;
}
