fun fib(n:int) -> int {
  if (n < 2)
    return n;
  return fib(n - 2) + fib(n - 1);
}

fun main() {


  print "What is your name ?";
  var name = io.readline();
  print "Hello "+name;
  return nil;
}