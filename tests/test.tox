class Math {
  foo:int;
  fn add(a:int,b:int) -> int {
      return a*b;
  }
}

fn main() -> int {
  let foo = Math {foo:10};
  let mul = "hello";
  
  
  print foo.foo;

  foo.foo = 20;

  print foo.foo;
  

  return 0;
}