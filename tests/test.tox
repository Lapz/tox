enum List {
    Cons(int,List),
    Nil
}

fn main() {
    let f =  List::Cons(1,List::Cons(1,List::Nil));

    match f {
        List::Cons(x,xy) => x,
        List::Nil => 0,
        x => 10,
        y => 10

    };
}