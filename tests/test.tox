/*fun fib(n:int) -> int {
  if n < 2 {
    return n;
  } else {
    return fib(n - 2) + fib(n - 1);
  }
}

class Toggle {
    state:bool;

    fun new(state:bool) -> Toggle {
        return Toggle {state:state};
    }

    fun value() -> bool {
      return this.state;
    }

    fun activate() -> Toggle {
      this.state = !this.state;
      return this;
    }
}*/



fun main(){
    "Hello World";
    "My Name is Lenard";
}
