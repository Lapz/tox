fn fib(n:int) -> int {
  if (n < 2) {
    return n;
  }else {
    return fib(n - 2) + fib(n - 1);
  }
}
  

fn main() {
  print "Hello what's your name";
  let string = "Hello"+" "+read();
  print string;
  return;
}