fun add(a:int,b:int) -> int {
    return a+b;
}

fun fib(n:int) -> int {
  if (n < 2) 
    return n;
  return fib(n - 2) + fib(n - 1);
}

fun main() -> int {

    var add = |a:int,b:int| -> int {
      return a+b;
    };
    print add(30);
    return 0;
}

