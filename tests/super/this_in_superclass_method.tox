class Base {
  init(a) {
    this.a = a;
  }
}

class Derived < Base {
  init(a, b) {
    super.init(a);
    this.b = b;
  }
}

let derived = Derived("a", "b");
print(derived.a); // expect: a
print(derived.b); // expect: b